# Gaps — v2 (s2-write, s2-edit, s2-bash, s2-wrap)

- **s2-bash-09** (`바탕화면 위젯 동작 확인`): needs a desktop mock showing a drag path (mouse
  grabs the slime, drops it, gravity pulls it down) plus a right-click exit menu. No real capture
  exists, and the schema has no generic "desktop widget" screen kind (only vscode/terminal/shot/
  chat/browser) to represent a free-floating draggable character. Left as
  `template: "illustration"` (`desktop-slime-widget`) — same gap w7 already flagged; still needs an
  actual picture.
