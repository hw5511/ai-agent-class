# Gaps — v1 (s1-env, s2-review, s2-claudemd, s2-read)

- `s2-read-08` (PDF 확장 수동 설치): a VS Code Extensions panel — search box + result list + Install
  button — with no matching schema field. `screen.vscode` only models explorer/editor/terminal, not
  an extensions search UI, and there is no real capture of this moment in `web/public/shots`. Left as
  `illustration` (`vscode-extensions-search`).
